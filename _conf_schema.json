{
  "everos_base_url": {
    "description": "EverOS 服务地址",
    "hint": "独立容器部署的默认地址为 http://127.0.0.1:8765",
    "type": "string",
    "default": "http://127.0.0.1:8765"
  },
  "enable_tools": {
    "description": "启用 LLM 工具",
    "hint": "开启后 Agent 可使用 everos_memorize / everos_recall / everos_learn 工具",
    "type": "bool",
    "default": true
  },
  "enable_webui": {
    "description": "启用插件内嵌 WebUI",
    "hint": "在 AstrBot 后台提供 /everos 管理界面",
    "type": "bool",
    "default": true
  },
  "standalone_webui_enabled": {
    "description": "启用独立 WebUI 服务器",
    "hint": "启用后插件自动启动独立 Web 服务器，访问 http://IP:18766 即可使用 Dashboard",
    "type": "bool",
    "default": true
  },
  "standalone_webui_host": {
    "description": "独立 WebUI 监听地址",
    "hint": "默认 0.0.0.0 表示监听所有网络接口",
    "type": "string",
    "default": "0.0.0.0"
  },
  "standalone_webui_port": {
    "description": "独立 WebUI 访问端口",
    "hint": "浏览器通过 http://IP:此端口 访问 Dashboard",
    "type": "int",
    "default": 18766
  },
  "app_id": {
    "description": "应用标识",
    "type": "string",
    "default": "astrbot"
  },
  "project_id": {
    "description": "项目标识",
    "type": "string",
    "default": "default"
  },
  "isolation_personas": {
    "description": "记忆隔离白名单（逗号分隔）",
    "hint": "在此列表里的人格使用独立记忆空间，列表外的人格共享全部记忆。例如：助手A,助手B",
    "type": "string",
    "default": ""
  },
  "force_memory_recall": {
    "description": "强制记忆检索（RAG 预处理）",
    "hint": "开启后，每次 LLM 被调用前会自动以用户消息为查询词检索 EverOS 记忆，并将上下文注入系统提示词。关闭则 LLM 自行决定是否调用记忆工具。",
    "type": "bool",
    "default": false
  },
  "extra_user_ids": {
    "description": "额外查询用户 ID（逗号分隔）",
    "hint": "Dashboard 使用此列表扩展记忆查询范围。当 LLM 工具以 QQ 号等自定义 user_id 保存记忆后，需在此添加对应 ID 才能被 Dashboard 检索到。例如：1638501774,1234567890",
    "type": "string",
    "default": ""
  },
  "boundary_token_limit": {
    "description": "边界检测 Token 上限",
    "hint": "对应 EverOS default.toml [boundary_detection].hard_token_limit。当缓冲区积累消息的 token 数超过此值时自动触发记忆提取。默认 65536（64K）。",
    "type": "int",
    "default": 65536
  },
  "boundary_msg_limit": {
    "description": "边界检测消息条数上限",
    "hint": "对应 EverOS default.toml [boundary_detection].hard_msg_limit。当缓冲区积累消息条数超过此值时自动触发记忆提取。默认 500。",
    "type": "int",
    "default": 500
  }
}
